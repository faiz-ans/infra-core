# Kubernetes overlay (unused on this site)

This path is reserved for a future Kubernetes/ArgoCD site. YAML here MAY include Ingress, Service, HPA, and operators.

**Materia must not apply this directory.** This site’s deploy source is `components/` kube-play YAML plus Quadlet `.kube` / `.container` units selected by the root `MANIFEST.toml`.
