#!/usr/bin/env bash
# Removed from Layer 0. Materia is optional: bootstrap/core/materia-enable.sh
echo "materia-install.sh is gone. Layer 0 uses podman-install.sh."
echo "Optional poller: sudo bash bootstrap/core/materia-enable.sh"
exit 1
