# Homepage config seed (phase A / new sites)
#
# This directory is the site-agnostic starter layout. It is NOT used by Compose
# unless you copy it into config/.
#
# New site:
#   mkdir -p stacks/platform/homepage/config
#   cp stacks/platform/homepage/config.seed/* stacks/platform/homepage/config/
#
# This reference site already has a customized config/ — leave it alone.
# Never replace config/ with config.seed on a customized site.
