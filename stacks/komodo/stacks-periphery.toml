# ResourceSync source for the remote Periphery host.
# Komodo does not interpolate [[VAR]] in server/repo — those must be literals.
# Apply this file only after the periphery server is connected.
# webhook_enabled is false; poll on-site.
# Storage transport is per stack: compose.yaml + DATA_ROOT (local / host NFS / host SMB),
# or compose.nfs.yaml + NAS_LAN_IP + NFS_EXPORT (Docker NFS driver). Do not list both files.

[[stack]]
name = "jellyfin"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/jellyfin"
file_paths = ["compose.nfs.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
PUID = [[PUID]]
PGID = [[PGID]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
"""

[[stack]]
name = "arr"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/arr"
file_paths = ["compose.nfs.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
PUID = [[PUID]]
PGID = [[PGID]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
"""

[[stack]]
name = "qbittorrent"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/qbittorrent"
file_paths = ["compose.nfs.yaml"]
config_files = [
  { path = "custom-cont-init.d/10-webui.sh", requires = "Redeploy" },
]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
PUID = [[PUID]]
PGID = [[PGID]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
"""

[[stack]]
name = "nextcloud"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/nextcloud"
file_paths = ["compose.nfs.yaml"]
config_files = [
  { path = "custom-cont-init.d/10-proxy.sh", requires = "Redeploy" },
]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
PUID = [[PUID]]
PGID = [[PGID]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
NEXTCLOUD_ADMIN_USER = [[NEXTCLOUD_ADMIN_USER]]
NEXTCLOUD_ADMIN_PASSWORD = [[NEXTCLOUD_ADMIN_PASSWORD]]
DOMAIN = [[DOMAIN]]
"""

# HA compose.nfs.yaml is the HTPC file; /config is a local volume + catalog bind, not NFS.
[[stack]]
name = "homeassistant"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/homeassistant"
file_paths = ["compose.nfs.yaml"]
config_files = [
  { path = "configuration.yaml", requires = "Redeploy" },
]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
"""

# Local volume. Same wildcard as Core (*.DOMAIN → NAS_LAN_IP / Caddy).
[[stack]]
name = "pihole"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/platform/pihole"
file_paths = ["compose.periphery.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
NAS_LAN_IP = [[NAS_LAN_IP]]
HTPC_UPSTREAM = [[HTPC_UPSTREAM]]
"""

[[stack]]
name = "monitoring"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/platform/monitoring"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
NAS_LAN_IP = [[NAS_LAN_IP]]
GRAFANA_ADMIN_PASSWORD = [[GRAFANA_ADMIN_PASSWORD]]
"""

# Off until the 4TB IronWolf is the backup target. Do not apply this stack yet.
[[stack]]
name = "restic-rest"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/platform/restic-rest"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
BACKUP_DRIVE = [[BACKUP_DRIVE]]
RESTIC_REST_USER = [[RESTIC_REST_USER]]
RESTIC_REST_PASSWORD = [[RESTIC_REST_PASSWORD]]
"""
