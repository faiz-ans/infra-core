# ResourceSync source for the remote Periphery host.
# Komodo does not interpolate [[VAR]] in server/repo — those must be literals.
# Apply this file only after the periphery server is connected.
# webhook_enabled is false; poll on-site.
# repo path stays faiz-ans/infra-core. After Gitea is up, the same Komodo git
# provider (gitea:3000) is used here; GitHub is a push mirror.
# Storage: /config is always a local volume. Household data is either
# compose.yaml + DATA_ROOT binds, or compose.nfs.yaml + NAS_LAN_IP + NFS_EXPORT (+ NFS_USERS for Immich).
# Do not list both files.

# compose.nfs.yaml: media on NFS; /config is local (survives Core down).
[[stack]]
name = "jellyfin"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/jellyfin"
file_paths = ["compose.nfs.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
PUID = [[PUID]]
PGID = [[PGID]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
"""

# Local volume only. Setup wizard talks to Jellyfin/Radarr/Sonarr over HTPC published ports.
[[stack]]
name = "seerr"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/seerr"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
"""

# compose.nfs.yaml: media/downloads on NFS; Sonarr/Radarr/Prowlarr /config is local (SQLite).
[[stack]]
name = "arr"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/arr"
file_paths = ["compose.nfs.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
PUID = [[PUID]]
PGID = [[PGID]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
"""

# compose.nfs.yaml: downloads/media on NFS; /config is local (survives Core down).
[[stack]]
name = "qbittorrent"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/qbittorrent"
file_paths = ["compose.nfs.yaml"]
config_files = [
  { path = "custom-cont-init.d/10-webui.sh", requires = "Redeploy" },
]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
PUID = [[PUID]]
PGID = [[PGID]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
"""

# compose.nfs.yaml: photos/users on NFS; Postgres, Redis, ML, and upload are local.
[[stack]]
name = "immich"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/immich"
file_paths = ["compose.nfs.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
NFS_USERS = [[NFS_USERS]]
IMMICH_DB_PASSWORD = [[IMMICH_DB_PASSWORD]]
"""

[[stack]]
name = "collabora"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/collabora"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
NAS_LAN_IP = [[NAS_LAN_IP]]
"""

# compose.nfs.yaml: events on NFS shared/cameras; MariaDB/config/log are local.
[[stack]]
name = "zoneminder"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/zoneminder"
file_paths = ["compose.nfs.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
PUID = [[PUID]]
PGID = [[PGID]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
ZM_DB_PASSWORD = [[ZM_DB_PASSWORD]]
"""

# Local volumes for PostGIS and media. Caddy proxies :8015.
[[stack]]
name = "adventurelog"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/adventurelog"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
ADVENTURELOG_POSTGRES_PASSWORD = [[ADVENTURELOG_POSTGRES_PASSWORD]]
ADVENTURELOG_ADMIN_PASSWORD = [[ADVENTURELOG_ADMIN_PASSWORD]]
"""

# HA /config is a local volume. trusted_proxies is written at container start (ensure-http/).
[[stack]]
name = "homeassistant"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/homeassistant"
file_paths = ["compose.nfs.yaml"]
config_files = [
  { path = "ensure-http/http.yaml", requires = "Redeploy" },
  { path = "ensure-http/patch.py", requires = "Redeploy" },
]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
NAS_LAN_IP = [[NAS_LAN_IP]]
"""

# Local volume. Same wildcard as Core (*.DOMAIN → NAS_LAN_IP / Caddy).
# ResourceSync matches stacks by name globally — must not reuse "pihole" from stacks-core.toml.
[[stack]]
name = "pihole-periphery"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/platform/pihole"
file_paths = ["compose.periphery.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
NAS_LAN_IP = [[NAS_LAN_IP]]
HTPC_UPSTREAM = [[HTPC_UPSTREAM]]
"""

# Read-only Docker API for Homepage. LAN bind only; not in Caddy.
[[stack]]
name = "dockerproxy"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/platform/dockerproxy"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
HTPC_UPSTREAM = [[HTPC_UPSTREAM]]
"""

[[stack]]
name = "monitoring"
deploy = true
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/platform/monitoring"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
NAS_LAN_IP = [[NAS_LAN_IP]]
GRAFANA_ADMIN_PASSWORD = [[GRAFANA_ADMIN_PASSWORD]]
"""

# Off until the 4TB IronWolf is the backup target. Do not apply this stack yet.
[[stack]]
name = "restic-rest"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/platform/restic-rest"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
BACKUP_DRIVE = [[BACKUP_DRIVE]]
RESTIC_REST_USER = [[RESTIC_REST_USER]]
RESTIC_REST_PASSWORD = [[RESTIC_REST_PASSWORD]]
"""
