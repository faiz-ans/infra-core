# GENERATED from topology.inc — do not hand-edit.
# Regenerate: python3 stacks/komodo/generate-stacks.py
#
# Phase B stacks for server "periphery" (excludes bootstrap phase).
# Apply after the periphery server is connected.
# deploy = false in fragments: ResourceSync updates definitions without mass-starting.
# webhook_enabled is false; poll on-site.
# Storage: /config is always a local volume. Household data: compose.yaml +
# DATA_ROOT, or compose.nfs.yaml (this site). Do not list two transport files.
# Docker Desktop: bootstrap/periphery-docker-engine.ps1 before applying.

[[stack]]
name = "jellyfin"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/jellyfin"
file_paths = ["compose.nfs.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
PUID = [[PUID]]
PGID = [[PGID]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
"""

# Local volume only. Setup wizard talks to Jellyfin/Radarr/Sonarr over HTPC published ports.

[[stack]]
name = "seerr"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/seerr"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
"""

# compose.nfs.yaml: media/downloads on NFS; Sonarr/Radarr/Prowlarr /config is local (SQLite).

[[stack]]
name = "arr"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/arr"
file_paths = ["compose.nfs.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
PUID = [[PUID]]
PGID = [[PGID]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
"""

# compose.nfs.yaml: downloads/media on NFS; /config is local (survives Core down).

[[stack]]
name = "qbittorrent"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/qbittorrent"
file_paths = ["compose.nfs.yaml"]
config_files = [
  { path = "custom-cont-init.d/10-webui.sh", requires = "Redeploy" },
]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
PUID = [[PUID]]
PGID = [[PGID]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
"""

# compose.nfs.yaml: photos/users on NFS; Postgres, Redis, ML, and upload are local.

[[stack]]
name = "immich"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/immich"
file_paths = ["compose.nfs.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
NFS_USERS = [[NFS_USERS]]
IMMICH_DB_PASSWORD = [[IMMICH_DB_PASSWORD]]
"""

[[stack]]
name = "adventurelog"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/adventurelog"
file_paths = ["compose.yaml"]
config_files = [
  { path = "entrypoint-oidc.sh", requires = "Redeploy" },
  { path = "sitecustomize-oidc.py", requires = "Redeploy" },
  { path = "seed-oidc.py", requires = "Redeploy" },
]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
NAS_LAN_IP = [[NAS_LAN_IP]]
ADVENTURELOG_POSTGRES_PASSWORD = [[ADVENTURELOG_POSTGRES_PASSWORD]]
ADVENTURELOG_ADMIN_PASSWORD = [[ADVENTURELOG_ADMIN_PASSWORD]]
OIDC_CLIENT_SECRET = [[OIDC_CLIENT_SECRET]]
"""

# Local volumes for SQLite and Whisper models. Caddy proxies :8085.

[[stack]]
name = "scriberr"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/scriberr"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
"""

# Local volume for uploads/SQLite. Caddy proxies :3313.

[[stack]]
name = "transmute"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/transmute"
file_paths = ["compose.yaml"]
config_files = [
  { path = "entrypoint-oidc.sh", requires = "Redeploy" },
  { path = "sitecustomize-oidc.py", requires = "Redeploy" },
]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
NAS_LAN_IP = [[NAS_LAN_IP]]
TRANSMUTE_AUTH_SECRET_KEY = [[TRANSMUTE_AUTH_SECRET_KEY]]
OIDC_CLIENT_SECRET = [[OIDC_CLIENT_SECRET]]
"""

# Local volume for Argos models. Caddy proxies :5000.

[[stack]]
name = "libretranslate"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/libretranslate"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
"""

# Local docstore volume. Caddy proxies :3003 (not SeaweedFS :8333).

[[stack]]
name = "openreader"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/openreader"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
OPENREADER_AUTH_SECRET = [[OPENREADER_AUTH_SECRET]]
"""

# Local SQLite volume. Caddy proxies :5678.

[[stack]]
name = "n8n"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/n8n"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
N8N_ENCRYPTION_KEY = [[N8N_ENCRYPTION_KEY]]
"""

# Recordings on NFS (shared/cameras). /config is local. Caddy proxies :8971.
# MQTT :1883 is LAN-only for Home Assistant (not in Caddy).

[[stack]]
name = "frigate"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/frigate"
file_paths = ["compose.nfs.yaml"]
config_files = [
  { path = "seed/config.yml", requires = "Redeploy" },
  { path = "mosquitto/mosquitto.conf", requires = "Redeploy" },
]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
NAS_LAN_IP = [[NAS_LAN_IP]]
NFS_EXPORT = [[NFS_EXPORT]]
"""

# HA /config is a local volume. trusted_proxies is written at container start (ensure-http/).

[[stack]]
name = "homeassistant"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/workload/homeassistant"
file_paths = ["compose.nfs.yaml"]
config_files = [
  { path = "ensure-http/http.yaml", requires = "Redeploy" },
  { path = "ensure-http/patch.py", requires = "Redeploy" },
]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
NAS_LAN_IP = [[NAS_LAN_IP]]
"""

# Host stats for Homepage. LAN bind :61208; Caddy proxies host2.<DOMAIN>.
# ResourceSync names are global — must not reuse "glances" from stacks-core.toml.

[[stack]]
name = "glances-periphery"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/platform/glances"
file_paths = ["compose.periphery.yaml"]
config_files = [
  { path = "glances.conf", requires = "Redeploy" },
]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
"""

# Local volume. Same wildcard as Core (*.DOMAIN → NAS_LAN_IP / Caddy).
# ResourceSync matches stacks by name globally — must not reuse "pihole" from stacks-core.toml.

[[stack]]
name = "pihole-periphery"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/platform/pihole"
file_paths = ["compose.periphery.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
CORE_SERVER = [[CORE_SERVER]]
NAS_LAN_IP = [[NAS_LAN_IP]]
HTPC_UPSTREAM = [[HTPC_UPSTREAM]]
PIHOLE_PERIPHERY_WEBPASSWORD = [[PIHOLE_PERIPHERY_WEBPASSWORD]]
"""

# Read-only Docker API for Homepage. LAN bind only; not in Caddy.

[[stack]]
name = "dockerproxy"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/platform/dockerproxy"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
HTPC_UPSTREAM = [[HTPC_UPSTREAM]]
"""

[[stack]]
name = "monitoring"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/platform/monitoring"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
DOMAIN = [[DOMAIN]]
NAS_LAN_IP = [[NAS_LAN_IP]]
GRAFANA_ADMIN_PASSWORD = [[GRAFANA_ADMIN_PASSWORD]]
OIDC_CLIENT_SECRET = [[OIDC_CLIENT_SECRET]]
"""

# Off until the 4TB IronWolf is the backup target. Do not apply this stack yet.

[[stack]]
name = "restic-rest"
deploy = false
[stack.config]
server = "periphery"
repo = "faiz-ans/infra-core"
branch = "main"
run_directory = "stacks/platform/restic-rest"
file_paths = ["compose.yaml"]
webhook_enabled = false
poll_for_updates = true
environment = """
TZ = [[TZ]]
BACKUP_DRIVE = [[BACKUP_DRIVE]]
RESTIC_REST_USER = [[RESTIC_REST_USER]]
RESTIC_REST_PASSWORD = [[RESTIC_REST_PASSWORD]]
"""
