[Unit]
Description=libretranslate

[Kube]
Yaml={{ m_outputDir }}/components/libretranslate/pod.yaml
Network=site.network
PublishPort=5000:5000

[Service]
Restart=on-failure

[Install]
WantedBy=default.target
