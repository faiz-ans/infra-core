[Unit]
Description=Site Podman network (replaces Docker edge)

# oneshot without this goes inactive; kube units that Require it then fail.
[Service]
RemainAfterExit=yes

[Network]
NetworkName=site
Driver=bridge

[Install]
WantedBy=default.target
