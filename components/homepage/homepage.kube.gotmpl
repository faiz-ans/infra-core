[Unit]
Description=homepage

[Kube]
Yaml={{ m_outputDir }}/components/homepage/pod.yaml
Network=site.network
PublishPort=127.0.0.1:3000:3000

[Service]
Restart=on-failure

[Install]
WantedBy=default.target
